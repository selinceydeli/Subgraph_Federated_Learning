{
  "reverse_mp_with_port_and_ego": {
    "model_name": "pna_reverse_mp_with_port_and_ego",
    "best_model_path": "./checkpoints/pna_reverse_mp_with_port_and_ego",

    "minority_class_weight_info": {
      "null": "no class weighting",
      "auto": "compute per-task class weights from training labels per sub-task",
      "number": "use uniform scalar pos_weight for all tasks (e.g. 2.0)"
    },

    "use_ego_ids": true,
    "use_port_ids": true,
    "use_mini_batch": true,
    "batch_size": 32,
    "port_emb_dim": 8,
    "num_epochs": 100,

    "default_hparams": {
      "ego_dim": null,
      "num_layers": 2,
      "neighbors_per_hop": [10, 4],
      "hidden_dim": 64,
      "dropout": 0.1,
      "lr": 0.001,
      "weight_decay": 0.0001,
      "minority_class_weight": "auto"
    }
  },

  "pna_baseline": {
    "model_name": "pna_baseline",
    "best_model_path": "./checkpoints/pna_baseline",

    "use_mini_batch": false,
    "num_epochs": 100,

    "default_hparams": {
      "hidden_dim": 64,
      "num_layers": 2,
      "dropout": 0.1,
      "lr": 0.001,
      "weight_decay": 0.0001,
      "minority_class_weight": null
    }
  }
}
